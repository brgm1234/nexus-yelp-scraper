{
  "title": "Yelp Scraper Input",
  "description": "Configuration for Yelp business scraping",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searches": {
      "title": "Search Terms",
      "type": "array",
      "description": "Search terms for Yelp (e.g., ['restaurants in NYC', 'plumbers in LA'])",
      "items": { "type": "string" },
      "editor": "stringList"
    },
    "maxResults": {
      "title": "Maximum Results",
      "type": "integer",
      "description": "Maximum number of results to scrape",
      "default": 20,
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": ["searches"]
}